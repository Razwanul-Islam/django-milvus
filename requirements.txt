pymilvus==2.4.10
django
numpy>=1.21

# Optional, for the caching layer:
#   pip install django-milvus[cache]   # redis, for the shared L2 tier
#   pip install django-milvus[fast]    # hnswlib, lz4, msgpack, psutil
